# Add project specific ProGuard rules here.
# minifyEnabled is currently false for the release build type, so this file
# is not applied yet, but is referenced by app/build.gradle.kts for when it is.
